% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotAge.R
\name{plot_length}
\alias{plot_length}
\title{Plot observed and predicted length distributions}
\usage{
plot_length(pars, length_freq, Delta_l = 1, l_max = NULL)
}
\arguments{
\item{pars}{A list containing model parameters to pass to
\code{solve_pde_steady_state()}: k, L_inf, d, m, r (and optionally vB_min_size),
as well as optional selectivity parameters l50 and l25 (or ratio).}

\item{length_freq}{A data frame with columns \code{length} and \code{count}, containing
observed length frequencies.}

\item{Delta_l}{Width of size bins (cm). Default is 1.}

\item{l_max}{The maximum length to consider. If NULL (default), it is set to
110\% of the maximum observed length.}
}
\value{
A \code{ggplot2} object comparing observed and predicted length distributions.
}
\description{
Compares the observed length distribution from data to the model prediction
from steady-state density, accounting for size selectivity.
}
\examples{
# Example using the Cod Celtic Sea data
data(Cod_CS_pars)
data(Cod_CS_length_freq)
# Without selectivity
plot_length(Cod_CS_pars, Cod_CS_length_freq)
# With selectivity
pars <- Cod_CS_pars
pars$l50 <- 40
pars$l25 <- 30
plot_length(pars, Cod_CS_length_freq)
}
